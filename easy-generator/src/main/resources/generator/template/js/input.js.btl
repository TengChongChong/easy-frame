//== ${config.businessName}-详情页
var ${GeneratorJsUtil.capitalLowercase(config.modelName)}Input = function () {

    return {
        //== 初始化页面
        init: function () {
            KTTool.setBaseUrl(basePath + '/auth/${config.controllerMapping}/');
        }
    };
}();
//== 初始化
${config.jquery}(document).ready(function () {
    ${GeneratorJsUtil.capitalLowercase(config.modelName)}Input.init();
});